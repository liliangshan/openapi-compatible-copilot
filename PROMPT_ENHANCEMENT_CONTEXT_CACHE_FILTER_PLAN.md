# 提示词优化中间消息缓存过滤方案

## 背景

开启提示词优化后，聊天消息流中可能会出现提示词优化流程产生的中间消息。例如：

```text
用户：原始请求
助手：提示词已优化，请再次发送或者修改
用户：[优化后提示词前缀] 优化后的请求
助手：真实回答
```

其中“提示词已优化，请再次发送或者修改”不是模型对用户业务问题的真实回答，而是扩展在提示词优化流程中返回给用户的操作提示。它上方最近的一条用户消息通常是触发本次提示词优化的原始请求。

提示词优化可能存在两种发送场景：

一、优化后只填充到输入框，不自动发送。此时会出现类似“提示词已优化，请再次发送或者修改”的助手提示。

二、优化后自动发送。此时可以让扩展也返回一条统一格式的助手提示，例如“提示词已优化：原内容”。这条消息同样只是提示词优化流程的中间产物，不是真实业务回答。

为了统一两种场景，缓存过滤逻辑不应只匹配完整固定句子，而应判断助手消息是否以“提示词已优化”对应的本地化前缀开头。只要某条助手消息以该前缀开头，就认为它是提示词优化中间消息，并过滤它和它上方最近的一条用户消息。

如果保存提示词上下文缓存时把这两条消息一起保存，会导致后续提示词优化读取缓存时混入中间流程内容，从而产生上下文污染。

## 问题

当前提示词上下文缓存保存流程主要保留用户消息和助手消息，并过滤系统消息、工具消息。

但是它没有识别提示词优化流程产生的中间助手消息，因此可能把以下内容保存到文件缓存：

```text
用户：原始请求
助手：提示词已优化，请再次发送或者修改
用户：[优化后提示词前缀] 优化后的请求
```

或者在自动发送场景中保存为：

```text
用户：原始请求
助手：提示词已优化：原内容
用户：[优化后提示词前缀] 优化后的请求
```

这会造成：

一、原始请求和优化后请求重复进入缓存。

二、“提示词已优化，请再次发送或者修改”这类流程提示被当作真实助手回答。

三、后续提示词优化模型读取缓存时，可能误解上下文。

四、缓存内容与真正主模型对话语义不一致。

## 目标规则

保存提示词上下文缓存时，应局部过滤提示词优化中间消息。

当发现某条助手消息以“提示词已优化”对应的本地化前缀开头时，只过滤：

一、这条助手提示消息本身。

二、它上方最近的一条尚未被过滤的用户消息。

不要把它作为全局分界线，不要删除它上方所有历史消息，因为该提示可能出现在对话中间，也可能出现多次。

## 示例

原始规范化消息：

```text
用户：正常问题甲
助手：正常回答甲
用户：原始请求乙
助手：提示词已优化，请再次发送或者修改
用户：[优化后] 优化后的请求乙
助手：真实回答乙
用户：正常问题丙
助手：正常回答丙
```

自动发送场景中也可能是：

```text
用户：正常问题甲
助手：正常回答甲
用户：原始请求乙
助手：提示词已优化：原始请求乙
用户：[优化后] 优化后的请求乙
助手：真实回答乙
用户：正常问题丙
助手：正常回答丙
```

保存到提示词上下文缓存时应变为：

```text
用户：正常问题甲
助手：正常回答甲
用户：[优化后] 优化后的请求乙
助手：真实回答乙
用户：正常问题丙
助手：正常回答丙
```

也就是只删除这一组中间产物：

```text
用户：原始请求乙
助手：提示词已优化，请再次发送或者修改
```

或自动发送场景中的这一组中间产物：

```text
用户：原始请求乙
助手：提示词已优化：原始请求乙
```

## 多语言要求

“提示词已优化，请再次发送或者修改”和“提示词已优化：原内容”这类提示会随着扩展语言设置不同而显示为不同语言。

因此过滤逻辑不能只匹配简体中文固定文本。

应基于扩展已有的本地化文案常量进行判断，或者维护一个覆盖所有支持语言的提示词优化完成消息前缀集合。

当前扩展支持的语言包括：

一、英文

二、简体中文

三、繁体中文

四、韩文

五、日文

六、法文

七、德文

过滤判断应覆盖所有语言对应的“提示词已优化”语义前缀。

## 推荐实现思路

一、在提示词上下文缓存写入前，对已经规范化后的用户和助手消息数组执行一次局部过滤。

二、增加一个函数，用于判断助手消息是否以提示词优化完成提示前缀开头。

三、该判断函数不应只写死简体中文，应使用所有语言对应文案前缀。

四、过滤算法从前到后扫描消息：

```text
如果当前消息是助手消息，并且内容以提示词优化完成提示前缀开头：
    标记当前助手消息删除
    向上查找最近一条未删除的用户消息
    标记该用户消息删除
最终返回所有未删除消息
```

五、如果连续出现多个提示词优化完成提示，每个提示都只删除自身以及它上方最近的一条未删除用户消息。

六、如果某条提示词优化完成提示上方找不到用户消息，则只删除该助手提示本身。

## 推荐放置位置

推荐在 `src/promptContextCache.ts` 中实现过滤，原因是提示词上下文缓存的所有入口最终都会进入这里：

一、`promptContextMessagesFromOpenAIMessages`

二、`promptContextMessagesFromVSCodeMessages`

三、`savePromptEnhancementContextCache`

更推荐在 `savePromptEnhancementContextCache` 内部、`normalizePromptContextMessages` 之后、`applyMessageLimit` 之前执行过滤。这样可以保证所有保存入口都统一生效。

## 注意事项

一、只过滤提示词上下文缓存，不影响主聊天历史保存，除非后续明确需要。

二、不要删除提示词优化提示下方的优化后用户消息，因为那才是真正应该进入主模型的有效请求。

三、如果优化后用户消息带有内部前缀，可以结合现有前缀清理逻辑决定是否在缓存中去掉该前缀。

四、过滤逻辑应支持同一会话中多次出现提示词优化流程。

五、过滤逻辑应尽量复用现有本地化文案，避免硬编码后续维护遗漏。
